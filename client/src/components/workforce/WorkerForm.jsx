// Purpose: Reusable validated worker creation/edit form.
export default function WorkerForm({ onSubmit, initialValues = {} }) { return <form className="page-form" onSubmit={onSubmit}><label>Full name<input name="name" defaultValue={initialValues.name || ''} placeholder="Full name" required /></label><label>Email<input name="email" type="email" defaultValue={initialValues.email || ''} placeholder="Email" required /></label><label>Employee ID<input name="employeeId" defaultValue={initialValues.employeeId || ''} placeholder="Employee ID" required /></label><button className="primary-button">Save worker</button></form>; }
